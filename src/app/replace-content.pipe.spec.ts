import { ReplaceContentPipe } from './replace-content.pipe';

describe('ReplaceContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceContentPipe();
    expect(pipe).toBeTruthy();
  });
});
