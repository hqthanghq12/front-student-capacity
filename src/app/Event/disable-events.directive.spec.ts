import { DisableEventsDirective } from './disable-events.directive';

describe('DisableEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableEventsDirective();
    expect(directive).toBeTruthy();
  });
});
