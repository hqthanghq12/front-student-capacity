import { RenderMathDirective } from './render-math.directive';

describe('RenderMathDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderMathDirective();
    expect(directive).toBeTruthy();
  });
});
